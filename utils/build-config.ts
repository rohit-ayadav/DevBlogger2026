export const BUILD_CONFIG = {
    // Set to true to generate static pages during build
    // Set to false to skip static generation (faster builds, useful for testing)
    GENERATE_STATIC_PAGES: false,
};
